# AIFATE 运势测算 Web 应用 - 产品需求文档 (PRD)

---

## 文档信息

| 项目名称 | AIFATE 运势测算 Web App |
|---------|------------------------|
| 文档版本 | V0.2 |
| 创建日期 | 2026-03-18 |
| 文档类型 | 产品需求文档 |
| 项目性质 | 娱乐应用 |

---

## 目录

1. [项目概述](#1-项目概述)
2. [项目目标](#2-项目目标)
3. [技术架构](#3-技术架构)
4. [功能需求](#4-功能需求)
5. [页面设计](#5-页面设计)
6. [核心算法](#6-核心算法)
7. [UI/UX 设计规范](#7-uiux-设计规范)
8. [组件架构](#8-组件架构)
9. [非功能需求](#9-非功能需求)
10. [文案策略](#10-文案策略)
11. [隐私与合规](#11-隐私与合规)

---

## 1. 项目概述

### 1.1 项目背景

AIFATE 是一款面向大众用户的运势测算娱乐工具，融合中国传统文化（八字）与西方神秘学（星座、塔罗），为用户提供趣味性的运势分析体验。

### 1.2 产品定位

* **产品类型**：Web 娱乐应用
* **目标用户**：对传统文化、神秘学感兴趣的年轻用户群体
* **使用场景**：日常娱乐、社交分享、好奇心驱动的探索
* **产品性质**：纯娱乐性质，不提供实际生活建议

---

## 2. 项目目标

### 2.1 核心目标

开发一个跨平台、响应式的运势测算 Web 应用，具备以下核心能力：

| 功能模块 | 描述 |
|---------|------|
| 输入模块 | 支持用户输入出生日期和出生时间 |
| 计算模块 | 基于当前时间进行多维度运势计算 |
| 输出模块 | 输出八字、星座、塔罗、综合运势等分析结果 |

### 2.2 平台支持

| 平台 | 要求 |
|-----|------|
| PC 浏览器 | Chrome、Firefox、Safari、Edge 等主流浏览器 |
| 移动端浏览器 | iOS Safari、Android Chrome 等移动浏览器 |
| 响应式设计 | Mobile First 策略，自适应不同屏幕尺寸 |

---

## 3. 技术架构

### 3.1 技术栈选型

#### 前端技术

| 技术 | 版本/规范 | 说明 |
|-----|----------|------|
| Next.js | App Router | React 框架，提供 SSR/SSG 能力 |
| TypeScript | - | 类型安全 |
| Tailwind CSS | - | 原子化 CSS 框架 |
| React | - | UI 组件库 |

#### 状态与数据

| 约束项 | 说明 |
|-------|------|
| 数据库 | 不使用 |
| 用户系统 | 不实现 |
| 数据存储 | 不存储用户数据 |
| localStorage | 仅用于 UI 状态管理 |

### 3.2 部署要求

```bash
# 项目可直接通过以下命令启动
npm install && npm run dev
```

### 3.3 架构原则

* **无状态设计**：不依赖后端存储
* **前端计算**：所有逻辑在前端完成
* **轻量化**：最小化依赖，快速加载
* **隐私优先**：不留存任何用户个人信息

---

## 4. 功能需求

### 4.1 功能清单

| 模块 | 功能 | 优先级 |
|-----|------|-------|
| 输入模块 | 日期/时间输入 | P0 |
| 八字模块 | 四柱计算、五行分布、文字解读 | P0 |
| 星座模块 | 星座计算、今日运势、三维运势 | P0 |
| 塔罗模块 | 随机抽牌、正逆位、翻牌动画 | P0 |
| 综合运势 | 评分系统、可视化展示、总结描述 | P0 |
| 建议模块 | 今日建议、桃花运、幸运数字、幸运颜色 | P0 |

### 4.2 功能详情

#### 4.2.1 输入模块

| 功能项 | 说明 |
|-------|------|
| 出生日期 | Date Picker 组件，支持日期选择 |
| 出生时间 | Time Picker 组件，支持时间选择 |
| 当前时间 | 自动获取并显示当前系统时间 |
| 开始测算 | 触发计算并跳转到结果页 |

#### 4.2.2 八字模块

| 功能项 | 说明 |
|-------|------|
| 四柱显示 | 年柱、月柱、日柱、时柱 |
| 五行分布 | 金、木、水、火、土的比例展示（进度条/图形） |
| 文字解读 | 基于四柱和五行的简要分析 |

#### 4.2.3 星座模块

| 功能项 | 说明 |
|-------|------|
| 星座计算 | 根据出生日期自动计算所属星座 |
| 今日运势 | 当日综合运势描述 |
| 三维运势 | 爱情、事业、财运三个维度的运势展示 |

#### 4.2.4 塔罗模块

| 功能项 | 说明 |
|-------|------|
| 随机抽牌 | 随机抽取 1~3 张塔罗牌 |
| 正逆位 | 每张牌显示正位或逆位状态 |
| 牌面含义 | 显示牌名和解读含义 |
| 翻牌动画 | 卡片翻转动画效果 |

#### 4.2.5 综合运势模块

| 功能项 | 说明 |
|-------|------|
| 运势评分 | 0~100 分的数值评分 |
| 可视化展示 | 进度条或仪表盘形式展示 |
| 总结描述 | 基于综合评分的文字总结 |

#### 4.2.6 建议模块

| 功能项 | 说明 |
|-------|------|
| 今日建议 | 当日行动建议 |
| 桃花期 | 桃花运势描述 |
| 幸运数字 | 1~9 之间的随机数字 |
| 幸运颜色 | 当日推荐幸运颜色 |

---

## 5. 页面设计

### 5.1 页面架构

```
┌─────────────────────────────────────────┐
│           首页 (/)                      │
│  ┌───────────────────────────────────┐  │
│  │     日期选择 + 时间选择           │  │
│  └───────────────────────────────────┘  │
│           [开始测算]                     │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│          结果页 (/result)               │
│  ┌───────────────────────────────────┐  │
│  │         八字卡片                  │  │
│  ├───────────────────────────────────┤  │
│  │         星座卡片                  │  │
│  ├───────────────────────────────────┤  │
│  │         塔罗卡片                  │  │
│  ├───────────────────────────────────┤  │
│  │       综合运势卡片                │  │
│  ├───────────────────────────────────┤  │
│  │         建议卡片                  │  │
│  └───────────────────────────────────┘  │
│        隐私提示 + 版权信息              │
└─────────────────────────────────────────┘
```

### 5.2 首页 (/)

#### 5.2.1 页面元素

| 元素 | 类型 | 说明 |
|-----|------|------|
| 标题 | 文本 | "运势测算" 或类似欢迎语 |
| 日期选择 | Date Picker | 出生日期输入 |
| 时间选择 | Time Picker | 出生时间输入 |
| 当前时间 | 文本 | 自动显示当前系统时间 |
| 开始测算按钮 | 按钮 | 提交并跳转 |

#### 5.2.2 交互逻辑

```
用户输入 → 表单验证 → 构造 URL → 跳转 /result
```

#### 5.2.3 URL 参数格式

```
/result?date=YYYY-MM-DD&time=HH:mm
```

### 5.3 结果页 (/result)

#### 5.3.1 布局结构

采用卡片式布局，每个模块独立成卡：

```
┌──────────────────────────────────────┐
│           八字模块                    │
├──────────────────────────────────────┤
│           星座模块                    │
├──────────────────────────────────────┤
│           塔罗模块                    │
├──────────────────────────────────────┤
│         综合运势模块                  │
├──────────────────────────────────────┤
│           建议模块                    │
└──────────────────────────────────────┘
```

#### 5.3.2 卡片内容

**八字卡片**
```
┌────────────────────────────────────┐
│  八字运势                         │
├────────────────────────────────────┤
│  年柱: XXXX                        │
│  月柱: XXXX                        │
│  日柱: XXXX                        │
│  时柱: XXXX                        │
├────────────────────────────────────┤
│  五行分布                          │
│  金: ████░░░░░░░░ 20%              │
│  木: ████████░░░░ 40%              │
│  水: ██░░░░░░░░░░ 10%              │
│  火: █░░░░░░░░░░░ 5%               │
│  土: ███░░░░░░░░░ 15%              │
├────────────────────────────────────┤
│  [解读文字...]                     │
└────────────────────────────────────┘
```

**星座卡片**
```
┌────────────────────────────────────┐
│  星座运势 - [星座名]               │
├────────────────────────────────────┤
│  今日运势: [描述...]               │
├────────────────────────────────────┤
│  爱情: ★★★★☆                       │
│  事业: ★★★☆☆                       │
│  财运: ★★★★★                       │
└────────────────────────────────────┘
```

**塔罗卡片**
```
┌────────────────────────────────────┐
│  塔罗指引                          │
├────────────────────────────────────┤
│  [卡1] [卡2] [卡3]                 │
│  (点击翻牌动画)                     │
├────────────────────────────────────┤
│  [卡1名称] [正位/逆位]              │
│  [含义解读...]                      │
└────────────────────────────────────┘
```

**综合运势卡片**
```
┌────────────────────────────────────┐
│  综合运势                          │
├────────────────────────────────────┤
│        ████████████░░ 78/100       │
├────────────────────────────────────┤
│  [总结性描述...]                   │
└────────────────────────────────────┘
```

**建议卡片**
```
┌────────────────────────────────────┐
│  今日建议                          │
├────────────────────────────────────┤
│  • 今日建议: [内容...]             │
│  • 桃花期: [内容...]               │
│  • 幸运数字: 7                     │
│  • 幸运颜色: 紫色                  │
└────────────────────────────────────┘
```

#### 5.3.3 底部隐私提示

所有页面底部必须显示：

```
本工具仅供娱乐参考，不构成任何实际建议，且不会存储您的个人信息
```

---

## 6. 核心算法

### 6.1 算法清单

| 函数名 | 位置 | 功能 |
|-------|------|------|
| `calculateBazi(date, time)` | `/lib` | 计算八字（四柱、五行） |
| `getZodiac(date)` | `/lib` | 根据日期计算星座 |
| `drawTarot()` | `/lib` | 随机抽取塔罗牌 |
| `generateFortune(allInputs)` | `/lib` | 生成综合运势评分 |

### 6.2 算法规范

#### 6.2.1 代码组织

所有核心算法函数统一放置在 `/lib` 目录下，遵循以下规范：

* 逻辑清晰，函数职责单一
* 代码可复用，避免重复
* 完善的注释说明
* TypeScript 类型定义

#### 6.2.2 函数签名

```typescript
// 八字计算
function calculateBazi(date: Date, time: string): BaziResult

// 星座计算
function getZodiac(date: Date): string

// 塔罗抽牌
function drawTarot(count: number): TarotCard[]

// 综合运势
function generateFortune(inputs: FortuneInputs): FortuneResult
```

#### 6.2.3 数据结构

```typescript
// 八字结果
interface BaziResult {
  yearPillar: string;    // 年柱
  monthPillar: string;   // 月柱
  dayPillar: string;    // 日柱
  hourPillar: string;    // 时柱
  elements: {            // 五行分布
    metal: number;
    wood: number;
    water: number;
    fire: number;
    earth: number;
  };
  interpretation: string; // 解读文字
}

// 塔罗牌
interface TarotCard {
  id: number;
  name: string;
  orientation: 'upright' | 'reversed';
  meaning: string;
}

// 综合运势
interface FortuneResult {
  score: number;          // 0-100
  summary: string;
  dailyAdvice: string;
  loveLuck: string;
  luckyNumber: number;
  luckyColor: string;
}
```

---

## 7. UI/UX 设计规范

### 7.1 设计风格融合

本项目融合三种文化/风格元素：

| 风格 | 元素 | 配色 |
|-----|------|------|
| 中国风 | 八卦、水墨、传统纹样 | 黑白灰、朱红、金色 |
| 西方星座 | 星空、行星、星座图 | 深蓝、星光银 |
| 塔罗 | 神秘符号、卡牌 | 紫色、金色 |

### 7.2 视觉规范

#### 7.2.1 配色方案

| 用途 | 颜色 |
|-----|------|
| 背景色 | 深色系 (#0f0f1a 或类似) |
| 卡片背景 | 半透明深色 |
| 主色调 | 紫色系 (#6b21a8) + 金色 (#f59e0b) |
| 文字 | 白色/浅灰 |
| 强调色 | 金色 |

#### 7.2.2 组件规范

| 组件 | 样式要求 |
|-----|---------|
| 卡片 | 圆角 (12-16px)、阴影、半透明背景 |
| 按钮 | 渐变背景、圆角、悬停效果 |
| 进度条 | 渐变色、圆角 |
| 输入框 | 深色背景、边框高亮 |

#### 7.2.3 字体规范

| 元素 | 字体 |
|-----|------|
| 标题 | 粗体、较大字号 |
| 正文 | 常规字体、易读字号 |
| 装饰文字 | 可使用书法风格字体 |

### 7.3 动效规范

| 动效 | 说明 |
|-----|------|
| 页面渐入 | 组件依次淡入，营造神秘感 |
| 塔罗翻牌 | 卡片 3D 翻转动画 |
| 加载动画 | 旋转或脉冲效果 |
| 悬停效果 | 卡片轻微上浮、阴影加深 |

---

## 8. 组件架构

### 8.1 组件目录结构

```
/components
├── BaziCard.tsx      # 八字卡片组件
├── ZodiacCard.tsx    # 星座卡片组件
├── TarotCard.tsx     # 塔罗卡片组件
├── SummaryCard.tsx   # 综合运势卡片组件
└── AdviceCard.tsx    # 建议卡片组件
```

### 8.2 组件职责

| 组件 | 职责 | Props |
|-----|------|-------|
| BaziCard | 展示八字信息 | `baziData: BaziResult` |
| ZodiacCard | 展示星座运势 | `zodiacData: ZodiacResult` |
| TarotCard | 展示塔罗牌及动画 | `tarotData: TarotCard[]` |
| SummaryCard | 展示综合运势 | `fortuneData: FortuneResult` |
| AdviceCard | 展示建议信息 | `adviceData: AdviceResult` |

### 8.3 组件设计原则

* 单一职责：每个组件负责一个功能模块
* 可复用：组件设计考虑复用性
* 类型安全：使用 TypeScript Props 定义
* 状态管理：简单状态使用 useState，复杂状态考虑 Context

---

## 9. 非功能需求

### 9.1 性能要求

| 指标 | 要求 |
|-----|------|
| 首屏加载时间 | < 3秒 |
| 页面响应时间 | < 500ms |
| 动画流畅度 | 60fps |

### 9.2 兼容性要求

| 平台 | 最低版本 |
|-----|---------|
| Chrome | 最新 2 个版本 |
| Safari | 最新 2 个版本 |
| Firefox | 最新 2 个版本 |
| Edge | 最新 2 个版本 |
| iOS Safari | iOS 14+ |
| Android Chrome | Android 10+ |

### 9.3 可访问性

* 支持键盘导航
* 适当的 ARIA 标签
* 色彩对比度符合 WCAG 标准

---

## 10. 文案策略

### 10.1 文案生成原则

所有运势解读文案需遵循以下原则：

| 原则 | 说明 |
|-----|------|
| 模板化 | 使用预设模板 + 随机组合生成 |
| 避免重复 | 随机种子确保不同输入产生不同结果 |
| 风格统一 | 神秘 + 积极引导 |
| 娱乐导向 | 明确标注仅供娱乐参考 |

### 10.2 文案风格

```
✓ 正面引导："今日适合做..."、"建议..."
✓ 神秘感："星辰指引..."、"命运流转..."
✗ 避免绝对化："一定会..."、"必定..."
✗ 避免负面："不要..."、"忌..."
```

### 10.3 模板示例

**八字解读模板**
```
"你的八字中{五行}能量较强，{特质}突出。
今日{月份}，{五行}当令，建议..."
```

**星座运势模板**
```
"{星座}座的朋友，今日{星象}，{运势描述}。
在{领域}方面，{具体建议}..."
```

**塔罗解读模板**
```
"抽到{牌名}{正逆位}，象征着{含义}。
这预示着{解读}..."
```

---

## 11. 隐私与合规

### 11.1 隐私保护

| 措施 | 说明 |
|-----|------|
| 无数据存储 | 不保存任何用户输入数据 |
| 无用户追踪 | 不使用用户行为追踪 |
| 无第三方 API | 不连接外部数据服务 |
| 无 Cookie | 不设置任何持久化 Cookie |

### 11.2 免责声明

所有页面必须显示免责提示：

```
本工具仅供娱乐参考，不构成任何实际建议，且不会存储您的个人信息
```

### 11.3 合规性

* 符合个人信息保护相关法规要求
* 明确产品娱乐性质
* 不提供迷信或伪科学内容作为实际指导

---

## 附录

### A. 技术术语

| 术语 | 解释 |
|-----|------|
| 八字 | 中国传统命理，根据出生时间计算年、月、日、时四柱 |
| 五行 | 金、木、水、火、土五种元素 |
| 星座 | 西方占星学中的黄道十二星座 |
| 塔罗 | 西方神秘学中的预测工具，使用一组卡片进行解读 |

### B. 版本历史

| 版本 | 日期 | 修改内容 |
|-----|------|---------|
| V0.1 | - | 初始版本 |
| V0.2 | 2026-03-18 | 本版本 |

---

**文档结束**
